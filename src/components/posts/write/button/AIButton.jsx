import * as s from "../../../../styles/posts/write/write";

const AIButton = ({ children }) => {
    return (
        <s.AIButtonContainer>
            {children}
        </s.AIButtonContainer>
    )
}

export default AIButton;